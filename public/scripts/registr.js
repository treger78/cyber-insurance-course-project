

/*
var express = require('express');
var bodyParser = require('body-parser');

*/







//window.require = require;
//const btn = document.getElementById('button');
/*
const mysql = require('../node_modules/mysql');

const connection = mysql.createConnection({
  database: "cyberInsurance",
  host: "localhost",
  user: "root",
  password: ""
});
*/
/*
	var secondName = document.getElementById('secondName');
	var firstName = document.getElementById('firstName');
	var patronymic = document.getElementById('patronymic');
	var birthDate = document.getElementById('birthDate');
	var mobilePhone = document.getElementById('mobilePhone');
	var email = document.getElementById('email');
	var password = document.getElementById('password');
	var passwordCheck = document.getElementById('passwordCheck');

btn.addEventListener('click', () => {
	secondName = secondName.value;
	firstName = firstName.value;
	patronymic = patronymic.value;
	birthDate = birthDate.value;
	mobilePhone = mobilePhone.value;
	email = email.value;
	password = password.value;
	passwordCheck = passwordCheck.value;
});
*/
/*
const sql = `INSERT INTO Users(secondName, firstName, patronymic, birthDate, 
				mobilePhone, email, password) VALUES('${secondName}', '${firstName}', '${patronymic}', ${birthDate}, 
				${mobilePhone}, '${email}', '${password}')`;

connection.end();
*/
//http-server -p 8000

//export {secondName, firstName, patronymic, birthDate, mobilePhone, email, password, passwordCheck};